# ขั้นตอนทดสอบ Code.gs กับชีตตัวอย่าง

แบ่งเป็น 3 ขั้น: (A) ทดสอบตรรกะในชีตทดสอบ ไม่ต้องใช้ LINE → (B) ทดสอบ endpoint จริงด้วย token ปลอม → (C) ทดสอบครบวงจรผ่าน LINE จริง

> ห้ามรันกับชีตจริงของสหกรณ์ `seedSampleData()` และ `runTests()` เขียน/ลบข้อมูลในชีต ให้ใช้ **File → Make a copy** สร้างสำเนาชีตเปล่าเพื่อทดสอบ

## A) ทดสอบตรรกะ (ไม่ต้องมี LINE)

1. สร้าง Google Sheet ใหม่ (ชีตทดสอบ) → Extensions → Apps Script
2. วางไฟล์ `Code.gs`, `Test.gs` และตั้ง `appsscript.json` (Project Settings → Show "appsscript.json")
3. ยังไม่ต้องตั้ง `LINE_CHANNEL_ID` (ขั้น A ข้ามการตรวจ token)
4. เลือกฟังก์ชัน `seedSampleData` → Run → กดอนุญาตสิทธิ์ (Review permissions) ครั้งแรก
   - ได้แท็บ `2567`, `2568` (หัวคอลัมน์ 28 ตัวจริง), `Form`, `Config` (2568 = เปิด, 2567 = ปิด)
   - สมาชิกตัวอย่าง: 1001 สมชาย ใจดี (ยอดสุทธิ 13,360.00), 1002 สมหญิง รักดี (9,700.00), 1003 ไม่มีเลขบัญชี
5. เลือกฟังก์ชัน `runTests` → Run → ดู **Execution log**
   - ต้องเห็น `สรุป: ผ่าน N / ไม่ผ่าน 0` และไม่มีบรรทัด `FAIL`
   - ถ้ามี FAIL จะ throw error ท้าย log บอกจำนวนข้อ

**ชุดทดสอบครอบคลุม**

| กลุ่ม | ตรวจอะไร |
|---|---|
| 1 ลงทะเบียน | ชื่อ/สังกัด/เลขสมาชิกไม่ตรง → `NOT_MATCH`, กรอกไม่ครบ → `BAD_INPUT`, ตัดคำนำหน้า+ช่องว่างแล้วตรง, userId ซ้ำ → `ALREADY_REGISTERED`, เลขสมาชิกซ้ำ → `MEMBER_TAKEN` |
| 2 Rate limit | ผิดครบ 5 ครั้งแล้วกรอกถูกก็ยังถูกบล็อก → `RATE_LIMITED` |
| 3 getData | ยอดรวม/หัก/สุทธิถูก, ซ่อนรายการหักที่เป็น 0, เลขบัญชีปิดบังและไม่มีเลขเต็มใน response, ปีไม่มี → `NO_DATA`, ปีผิดรูปแบบ → `BAD_INPUT`, ยังไม่ลงทะเบียน → `NOT_REGISTERED` |
| 4 แยกรายคน | สมาชิกแต่ละคนเห็นเฉพาะแถวของตัวเอง |
| 5 confirm | ปีที่ปิด → `CLOSED`, ยืนยันสำเร็จ, ซ้ำ → `ALREADY_CONFIRMED`, บันทึกวันเวลา พ.ศ. และ userId ลงชีต |
| 6 สวิตช์เลขบัญชี | `REQUIRE_BANK_LAST4=true` → ไม่มีเลขบัญชีในชีต `BANK_MISSING`, 4 ตัวท้ายผิด `NOT_MATCH` |

หลังรันเสร็จ `runTests` ล้างแถว `Utest*` ใน Form และค่ายืนยันในแท็บ 2568 ให้เอง รันซ้ำได้

**ตรวจด้วยตาเพิ่ม** (ตรวจได้เฉพาะดูชีต)
- หลังรัน `runTests` (ก่อนล้าง ให้คอมเมนต์บรรทัด `t_reset_();` ท้ายฟังก์ชันชั่วคราว) แท็บ 2568 แถว 1001 ต้องมีคอลัมน์ท้าย `วันเวลายืนยัน`, `ยืนยันโดย` และ Form มีแถว `Utest-A`, `Utest-D` พร้อม Timestamp

## B) ทดสอบ endpoint จริง (token ปลอม)

1. ตั้ง Script Properties: `LINE_CHANNEL_ID` = Channel ID จริง
2. Deploy → New deployment → Web app (Execute as: Me / Anyone) → คัดลอก URL `/exec`
3. เปิดเบราว์เซอร์ไปที่ URL นั้น (GET) ต้องได้ `{"ok":true,"service":"dividend-api"}`
4. ยิง POST ด้วย token ปลอม ต้องได้ `{"ok":false,"code":"TOKEN_INVALID",...}`

```bash
curl -L -X POST "URL_/exec_ของคุณ" -H "Content-Type: text/plain;charset=utf-8" -d '{"action":"init","idToken":"fake"}'
```

5. ลองไม่ส่ง idToken → `TOKEN_INVALID`, ส่ง JSON เสีย → `BAD_REQUEST`, ส่ง action มั่ว (ต้องมี token จริงจึงถึงขั้นนี้) → `BAD_ACTION`

ขั้นนี้ยืนยันว่า deploy ถูกต้องและคำขอที่ไม่มี token จริงเข้าข้อมูลไม่ได้ (`-L` จำเป็นเพราะ Apps Script redirect)

## C) ทดสอบครบวงจรผ่าน LINE

1. ตั้งค่า LINE Login channel + LIFF ตาม `README-ตั้งค่า.md` แก้ `LIFF_ID`/`API_URL` ใน `web/index.html` แล้วโฮสต์
2. เปิด `https://liff.line.me/<LIFF_ID>` ในแอป LINE ด้วยบัญชีทดสอบ
3. เดินตามลำดับแล้วติ๊กผล

| ขั้น | คาดหวัง |
|---|---|
| เปิดครั้งแรก | เห็นฟอร์มลงทะเบียน |
| กรอกผิด 1 ช่อง | SweetAlert "ข้อมูลไม่ตรงกับทะเบียนสมาชิก" |
| กรอกถูก (เช่น 1001) | "ลงทะเบียนสำเร็จ" → มีแถวใหม่ในชีต Form ที่ userID เป็นค่าขึ้นต้น `U` |
| เลือกปี 2568 | เห็นการ์ดยอด ปุ่ม "ยืนยันข้อมูล" |
| เลือกปี 2567 | ป้าย "ยังไม่เปิดรับการยืนยัน" |
| กดยืนยัน 2568 | Dialog ยืนยัน → สำเร็จ → ชีตมีค่า `ยืนยันแล้ว` วันเวลา และ userId |
| เปิดใหม่อีกครั้ง | ไม่ต้องลงทะเบียนซ้ำ ปีนั้นขึ้น "ยืนยันข้อมูลแล้ว" |
| บัญชี LINE อื่น ลงทะเบียน 1001 | "เลขที่สมาชิกนี้ถูกผูกกับบัญชี LINE อื่นแล้ว" |
| ลบแถวใน Form แล้วเปิดใหม่ | กลับไปหน้าลงทะเบียน (จำลองการเปลี่ยนบัญชี LINE) |
| ตั้ง Config 2568 = ปิด แล้วเปิดหน้า | ปุ่มยืนยันหาย (ถ้ายังไม่ยืนยัน) |
| รอ ID Token หมดอายุ (~1 ชม.) แล้วกดโหลดปี | SweetAlert "กรุณาเข้าสู่ระบบใหม่" แล้วพาไป login |

**ปัญหาที่พบบ่อย**
- `TOKEN_INVALID` ทั้งที่ login แล้ว → LIFF scope ยังไม่ติ๊ก `openid`, หรือ `LINE_CHANNEL_ID` ไม่ใช่ของ LINE Login channel ตัวเดียวกับที่ผูก LIFF
- หน้าเว็บขึ้น "เชื่อมต่อเซิร์ฟเวอร์ไม่ได้" → `API_URL` ผิด/ไม่ใช่ `/exec`, หรือ Deploy ไม่ได้ตั้ง Anyone
- แก้ `Code.gs` แล้วพฤติกรรมไม่เปลี่ยน → ต้อง Deploy → Manage deployments → Edit → New version
- ยืนยันไม่ได้ทั้งที่ปีเปิด → ชื่อแท็บใน Config ต้องเป็นตัวเลขล้วน (ไม่มีช่องว่าง/มีเครื่องหมาย ') และคอลัมน์ B เป็น `เปิด`
